org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Publishing coordinates (JitPack picks these up automatically from the repo,
# Maven Central needs GROUP/VERSION here)
GROUP=com.easypdfkit
VERSION_NAME=1.0.0
